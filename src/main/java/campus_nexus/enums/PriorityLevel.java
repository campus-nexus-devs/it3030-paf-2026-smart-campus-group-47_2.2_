package campus_nexus.enums;

/**
 * PriorityLevel enum defines the severity of the maintenance issue.
 * Used to categorize tickets for faster response times.
 */
public enum PriorityLevel {
    LOW,
    MEDIUM,
    HIGH
}
